# Dependencias de Node (se instalan con npm install)
node_modules/

# Base de datos local (contiene vault cifrado + metadatos, NO subir a GitHub)
supreme_key.db
supreme_key.db.*
*.verify-bak

# Logs y temporales
*.log
.DS_Store
Thumbs.db

# Archivos de verificacion ad-hoc de Hermes (en Temp, por si acaso)
hermes-verify-*

# Carpeta antigua server/ (estructura previa con rootDir=server). El codigo
# vive en la RAIZ del repo; esta carpeta es un residuo que confunde al clonar
# en otro equipo. Ignorada a proposito.
server/

# Bases de prueba generadas por los e2e (no subir)
f2-test.db
it-test.db
pp-test.db
cd-test.db
full-test.db
team-verify.db
t1.db
t2.db
t3.db
